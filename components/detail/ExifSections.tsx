// TODO: breaking change — ExifSections used MediaExif from exif.ts which has been deleted.
// Rewrite to accept PhotoMetadata from lib/media-types.ts once PhotoDetail and HdrDetail are updated.

// import { Container, Row } from "react-bootstrap";
// import type { MediaExif } from "./exif";
// import { formatBytes, formatDate, formatShutter } from "./format";
// import MetaTile from "./MetaTile";
//
// interface ExifSectionsProps {
//   exif: MediaExif;
//   file: { name: string; size: number };
// }
//
// export default function ExifSections({ exif, file }: ExifSectionsProps) { ... }

export default function ExifSections(_props: unknown) {
  return null;
}
